// Com o React Native voltamos ao tempo de quando tudo na web era formado por divs
// através do uso das tags View e Text

// O mesmo vale para o text, que não tem variedade como h1, h2, p, etc
// Basicament todo Text é Text

// Não há ids e classes para os elementos
// A estilização ocorre dentro do atributo style, que pode ser escrita em forma de objeto também
// No entanto, o React Native tem uma classe chamada StyleSheet, conseguindo realizar a criação dos estilos

// To dos os elementos do React Native já vem com display flex por padrão. Não existe display block e afins