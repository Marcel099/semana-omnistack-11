import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Um componente no React nada mais é do que uma função que retorna HTML 
// Geralmente os componentes do React vem com letra maiúscula
ReactDOM.render(<App />, document.getElementById('root'));
